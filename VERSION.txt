Football CEO v0.15.1 — Save Management

- Up to 5 independent local career save slots
- Autosave remains active after game actions / Continue
- Best-effort autosave when Safari tab is hidden or closed
- Visible last-saved status in the club masthead
- Manual Save button
- Saved Careers list on the start screen
- Load, export and delete controls for each local career
- JSON export backups for Files/iCloud
- JSON import/restore
- Existing legacy footballCEO2526 local save is migrated automatically when possible
- Starting a New Career no longer deletes the current career
- Save data remains scoped to the current browser + GitHub Pages origin
